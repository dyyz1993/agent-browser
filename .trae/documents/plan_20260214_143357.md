## 计划

基于当前代码状态，我建议暂时停止修改代码，专注于验证现有功能是否正常工作。

### 当前状态
1. **核心修改已完成**：
   - `getFrame(framePath?: string)` 统一方法已实现
   - `getFrameByPath()` 改为私有方法
   - `findMatchingFrame()` 支持索引/名称/URL 匹配
   - 移除了不必要的 `await`
   - 设置了全局超时为 5 秒

2. **测试状态**：
   - 已通过：12/21 测试
   - 待验证：9/21 测试（部分可能因断言缺失或超时问题失败）

### 建议下一步
1. **先运行完整测试**，确认哪些测试失败
2. **分析失败原因**，是断言缺失还是功能问题
3. **逐个修复**，而不是批量修改

是否要我先运行完整测试来查看当前状态？