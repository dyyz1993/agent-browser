### 修复步骤

1. **统一 Command 类型**
   - 修改 `src/__tests__/utils/parseCli.ts`，从 `types.ts` 导出 `Command` 类型
   - 删除本地的 `Command` 接口定义

2. **修复 Response.data 访问问题**
   - 创建类型守卫函数 `isSuccessResponse<T>(response: Response<T>): response is SuccessResponse<T>`
   - 或在测试中使用类型断言前先检查 `success` 属性

3. **修复 E2E 测试文件**
   - 使用正确的类型导入
   - 使用类型守卫或正确的类型断言方式访问 `data`

4. **验证修复**
   - 运行 `npm run typecheck` 确认无类型错误
   - 运行 `npm run test` 确认所有测试通过