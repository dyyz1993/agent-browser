# Session: r3a
Date: 2026-04-30
Round: 3 (Edge Case Testing)
Module: shadow-dom
Scenarios: 4 passed / 0 failed

## Tested
- Test A (Empty fields): PASSED — validation errors shown gracefully, no crash
- Test B (Special characters / XSS): PASSED — script tag stored as text, validation blocked submission, no injection
- Test C (Very long values): PASSED — 200-char name + 115-char email handled without crash/overflow
- Test D (Normal recovery): PASSED — form works correctly after all edge case abuse

## Findings
- `fill ""` (empty string) is NOT accepted by agent-browser CLI — use eval to clear fields instead
- Name regex validation (`/^[a-zA-Z\s]+$/`) effectively blocks XSS script tags in name field
- Special characters in email/password are stored as literal text, not interpreted as HTML/JS
- Long values beyond max length are caught by same validation regex (name) or format check (email)
- Form state fully recovers after edge case abuse — no lingering corruption
- eval operations are NOT recorded in `history --json` (only fill/click/select are logged)
- All stable selectors from R1/R2 worked correctly in R3

## Selectors Used (all from R1/R2 knowledge, verified stable)
- #name, #email, #password, #role (inside Shadow DOM)
- #external-submit (main document)

## Screenshots
- /tmp/r3a-edge-case-final.png (Test D success state)

## Updated
- Added edge_case_tips to patterns.yml
- No selector changes needed — all R1/R2 selectors remain valid
